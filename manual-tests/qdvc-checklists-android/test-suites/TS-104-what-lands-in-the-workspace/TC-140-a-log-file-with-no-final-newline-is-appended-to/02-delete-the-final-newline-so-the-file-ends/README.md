---
kind: test-step
sequence: 2
---

### Delete the final newline so the file ends immediately after the last row's text, then save

Most editors show this as the cursor sitting at the end of the last line with no empty line beneath it.

#### Expected result

The file no longer ends with a line break.
